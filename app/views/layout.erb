<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>Mallard</title>
    <%= css :application, :media => 'screen' %>
  </head>
  <body data-mobile-browser="<%= mobile_browser?.to_s %>">
    <div class="navbar navbar-inverse navbar-fixed-top" role="navigation">
      <div class="container">
        <div class="navbar-header">
          <button type="button" class="navbar-toggle" data-toggle="collapse" data-target=".navbar-collapse">
            <span class="sr-only">Toggle navigation</span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
          </button>
          <a class="navbar-brand" href="/">Mallard</a>
        </div>
        <div class="collapse navbar-collapse">
          <ul class="nav navbar-nav">
            <li><a href="/">Unread - <span class="unread-count"><%= EntryRepo.unread.count %></span></a></li>
            <li><a href="/entries/starred">Starred - <span class="starred-count"><%= EntryRepo.starred.count %></span></a></li>
            <li><a href="/feeds">Manage Feeds</a></li>
            <li><a href="/entries/read">Read Entries</a></li>
          </ul>
        </div>
      </div>
    </div>

    <div class="container-fluid">
      <% flash.each do |key, value| %>
        <div class="alert alert-<%= key.to_s %>"><%= value %></div>
      <% end %>
      <%= yield %>
    </div>

    <%= js :application %>
  </body>
</html>
